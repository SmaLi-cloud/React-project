const timeout = 5000;
const prefix = 'http://localhost:8000';
const isDebug = true;
const authority = ["co", "co.user", "co.staff", "welcome"];

const config = {
    timeout,
    prefix,
    isDebug,
    authority,
  };
export default config;
